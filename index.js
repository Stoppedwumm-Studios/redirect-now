const express = require("express")
const app = express()

app.get("/", (req,res) => {

})

app.get("/create-redirect", (req, res) => {
    const url = req.query.url

    
})

app.listen(3000, () => {

})